{{!-- <article class="post">
  <div class="post-head">  
    <div class="title">
      <a href="/post/{{id}}">{{title}}</a>
    </div>
    <div class="meta">
      Posted By {{user.username}} on {{format_date created_at}} | 
      {{comments.length}} {{format_plural "comment" comments.length}}
    </div>
  </div>
  <div class="post-text">
    {{post_text}}
  </div>
</article> --}}

<div class="card mt-2 mb-2">
  <h5 class="card-header">
    <a class="text-decoration-none" href="/post/{{id}}">{{title}}</a>
  </h5>
  <div class="card-body">
    <p class="card-text"><b>Posted By
        {{user.username}}
        on
        {{format_date created_at}}
        |
        {{comments.length}}
        {{format_plural "comment" comments.length}}</b></p>
    <p class="card-text">{{post_text}}</p>
    {{!-- <a href="/post/{{id}}" class="btn btn-primary">Comments</a> --}}
  </div>
</div>